name: rj_form_engine
description: >-
  Schema-driven dynamic form engine for Flutter with validation, rich field types, and minimal dependencies.
version: 0.2.1
homepage: https://github.com/ReturajProshad/rj_form_engine
repository: https://github.com/ReturajProshad/rj_form_engine
issue_tracker: https://github.com/ReturajProshad/rj_form_engine/issues

topics:
  - forms
  - form-builder
  - dynamic-forms
  - validation
  - cascading-dropdowns

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
